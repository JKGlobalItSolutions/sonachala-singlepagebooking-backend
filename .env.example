# MONGO_URI=mongodb://localhost:27017/hotelbooking
MONGO_URI=mongodb+srv://your_mongo_connection_string_here
JWT_SECRET=your_jwt_secret_key
PORT=5000

# BREVO (SendinBlue) SMTP Configuration
# Get these values from your Brevo account:
# 1. Go to Settings -> SMTP & API
# 2. Create SMTP keys if you haven't
# 3. BREVO_USER: Your SMTP login (usually your email)
# 4. BREVO_PASSWORD: Your master password (or use API key for password)
BREVO_USER=your_brevo_smtp_username
BREVO_PASSWORD=your_brevo_smtp_api_key

# Sender email address - Must be a verified email in Brevo
# This is what recipients see as the "from" address
EMAIL_USER=noreply@yourdomain.com

# The transporter now uses BREVO_USER and BREVO_PASSWORD for authentication
# EMAIL_USER is only used for the "from" field in emails
